import { View, Text } from "react-native";

const index = () => {
  return (
    <View>
      <Text className="text-red-800 text-3xl">index</Text>
    </View>
  );
};

export default index;
